# GitHub web engine snapshot

Frozen from `994cc73` (`feat(ios): prompt to update on first launch (#36)`),
tag `backup/pre-grok-merge-2026-08-21`.

This is the web control center rotation engine, contracts, and README **before**
Grok live rotators, the `global-api-keys` parser, and the Mac agent were folded
in. Live code is `apps/web/`.
