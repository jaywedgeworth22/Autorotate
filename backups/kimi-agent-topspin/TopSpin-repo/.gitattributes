# Normalize line endings to LF on commit
* text=auto eol=lf

# Xcode project files: merge union to reduce conflicts
*.pbxproj merge=union

# Treat these as binary
*.png binary
*.jpg binary
*.ico binary
